server {
  listen 80;
  listen [::]:80;
  server_name _;
  server_tokens on;

  root /usr/share/nginx/html;
  autoindex on;
  autoindex_exact_size off;
  autoindex_localtime on;

  location / {
    try_files $uri $uri/ =404;
  }
}
